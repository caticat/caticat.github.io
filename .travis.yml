language: node_js
node_js: stable

install:
  - npm install hexo --save
  - npm install hexo-leancloud-counter-security --save
  - npm install gitalk --save
  - npm install hexo-generator-searchdb --save
  - npm install hexo-generator-sitemap --save
  - npm install hexo-generator-baidu-sitemap --save
  - npm install hexo-helper-live2d --save
  - npm install live2d-widget-model-haru --save
  #- npm install -g hexo-cli

script:
  - cp -f node_modules/live2d-widget-model-haru/package.json node_modules/live2d-widget-model-haru/01/package.json
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - echo "# Pan's Blog" >> README.md
  - echo "![travis](https://travis-ci.org/caticat/caticat.github.io.svg?branch=source)" >> README.md
  - cp ../search_auth/baidu_verify_c5pg0Ve7oy.html .
  - cp ../search_auth/google4b09503098e8cf15.html .
  - git config user.name "caticat"
  - git config user.email "catifish@163.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches:
  only:
    - source

env:
  global:
    - GH_REF: github.com/caticat/caticat.github.io.git
