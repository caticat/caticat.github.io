# Archived Hexo Source

This branch contains the previous Hexo blog source and is retained only for historical reference.

The active site source is now maintained at:

- https://github.com/caticat/live2d-companion-page

The Travis deployment has been removed and deployment settings in `_config.yml` are disabled.

Legacy third-party credentials appeared in Git history. Removing them from the latest branch state does not make them safe. Revoke or rotate the old LeanCloud credentials and GitHub OAuth application secret before reusing any related integration.
