# Archived NexT theme configuration
#
# The previous configuration contained legacy LeanCloud and GitHub OAuth
# credentials. It has been replaced in the branch tip because this Hexo site is
# no longer deployed.
#
# Removing values from the latest file does not remove them from Git history.
# Revoke or rotate the affected credentials before reusing those integrations.

# The active site source is maintained at:
# https://github.com/caticat/live2d-companion-page

gitalk:
  enable: false
  owner: caticat
  repo: caticat.github.io
  clientID: REVOKED
  clientSecret: REVOKED

leancloud_visitors:
  enable: false
  app_id: REVOKED
  app_key: REVOKED
